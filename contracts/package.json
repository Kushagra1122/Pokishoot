{
  "name": "pokewars-contracts",
  "version": "1.0.0",
  "description": "PokemonNFT smart contracts for PokeWars",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:moonbase": "hardhat run scripts/deploy.js --network moonbase",
    "deploy:moonbeam": "hardhat run scripts/deploy.js --network moonbeam",
    "deploy:all": "hardhat run scripts/deploy-all.js --network moonbase",
    "deploy:all:mainnet": "hardhat run scripts/deploy-all.js --network moonbeam",
    "verify": "hardhat verify"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.3.1"
  }
}

